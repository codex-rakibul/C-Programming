#include <stdio.h>

int main()
{
   int w=5, h=7, a1=(w+h)*2, a2=(w*h);

   printf("Perimeter of the rectangle = %d inches\n",a1);
   printf("Area of the rectangle      = %d square inches",a2);

   return 0;
}
