#include <stdio.h>

   main()
{
   int x,a, b, c,d,e,f;

   printf("Input a,b,c,d,e,f number =");
   scanf("%d%d%d%d%d%d",&a,&b,&c,&d,&e,&f);

   x=pow(a,b)+pow(c,d)+pow(e,f);
   printf("\*****************\n X=%d",x);

}
